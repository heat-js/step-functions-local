{
  "name": "@heat/step-functions-local",
  "version": "0.0.2",
  "license": "MIT",
  "main": "src/index.js",
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/heat-js/step-functions-local.git"
  },
  "bugs": {
    "url": "https://github.com/heat-js/step-functions-local/issues"
  },
  "scripts": {
    "test": "./node_modules/.bin/jest",
    "patch": "yarn publish --patch --access=public",
    "minor": "yarn publish --minor --access=public",
    "major": "yarn publish --major --access=public",
    "deploy": "yarn publish --access=public"
  },
  "jest": {
    "testEnvironment": "node",
    "testRegex": "tests?/.*\\.(js)$"
  },
  "devDependencies": {
    "jest": "^24.8.0"
  }
}
